# ── Frontend (Vite) — these ship in the built bundle, so PUBLIC values only ──
# Reusing the existing Arete Supabase project (yanaxjuqqhvnrpqwlusb):
VITE_SUPABASE_URL=https://yanaxjuqqhvnrpqwlusb.supabase.co
# The PUBLISHABLE / anon key (sb_publishable_… or the legacy anon JWT). Public + RLS-protected.
VITE_SUPABASE_ANON_KEY=
# Full URL of the deployed Edge Function that handles gated writes + test sends:
VITE_FUNCTIONS_URL=https://yanaxjuqqhvnrpqwlusb.supabase.co/functions/v1/birthday-reminder

# NOTE: leave all three blank to run the app in local DEMO mode (browser-only,
# no backend, no access code) — handy for previewing the UI before wiring Supabase.
